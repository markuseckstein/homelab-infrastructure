# Voice Assistant Stack Environment Variables (Optional)
# Copy to .env file and customize if needed

# Ollama Configuration
OLLAMA_HOST=assi-ollama-cpu
OLLAMA_PORT=11434
OLLAMA_MODEL=phi

# MQTT Configuration
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_TOPIC_TEXT=voice/text
MQTT_TOPIC_INTENT=voice/intent

# Whisper Configuration (for future customization)
# WHISPER_MODEL=tiny      # Options: tiny, base, small
# WHISPER_LANGUAGE=de     # Primary language
# WHISPER_DEVICE=cpu      # Options: cpu, cuda, mps

# Debug (set to 1 to enable debug logging in all services)
DEBUG=0
